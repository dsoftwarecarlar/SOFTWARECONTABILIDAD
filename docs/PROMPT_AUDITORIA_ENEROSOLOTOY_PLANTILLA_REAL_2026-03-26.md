Auditar `C:\xampp\htdocs\SOFTWARECONTABILIDAD\resources\cxp\servicios_marcas\enerosolotoy` usando como verdad la plantilla manual `11. Concili. Servicios TYT enero 2026palntilla.xls`. Procesar exactamente esos TXT/XLS/XLSX y comparar en salida las celdas visibles `REP FACTURACIÓN!D9:E9:J9:K9`, `NOTA DE CREDITO!D4:G4` y `REP VTAS!D6:E6` contra la plantilla manual. Si hay desvíos, corregir la lógica para que los controles visibles salgan del origen correcto: ventas con ajuste PX, notas reconstruidas desde netos + descuento, y costo visible desde el control real de la plantilla cuando el lote mensual no trae un mayor de costos completo. Validar el mismo ajuste en `runtime.py`, `run_servicios_marcas.ps1` y pruebas automáticas.
